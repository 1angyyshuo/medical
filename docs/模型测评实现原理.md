# 模型测评实现原理

## 1. 整体架构

评测系统的核心链路：

```
HuggingFace 数据集 → 多选题 Prompt 构造 → 模型推理 → 答案提取 → 准确率计算 → 消融分析
```

评测脚本：[scripts_project/05_run_eval.py](../scripts_project/05_run_eval.py)
结果收集：[scripts_project/06_collect_results.py](../scripts_project/06_collect_results.py)

---

## 2. 数据集：ceval/ceval-exam

### 2.1 数据来源

CEval 是一个中文综合能力评测基准，覆盖 52 个学科。本项目的医学子集来自 HuggingFace 上的 [`ceval/ceval-exam`](https://huggingface.co/datasets/ceval/ceval-exam)，包含三个医学相关子集：

| 子集 | 含义 | 题型 | 典型规模 |
|---|---|---|---|
| `basic_medicine` | 基础医学（生理、病理、药理、解剖等） | 四选一 | ~200题 |
| `clinical_medicine` | 临床医学（诊断、治疗、各科室） | 四选一 | ~200题 |
| `physician` | 医师资格考试真题 | 四选一 | ~200题 |

### 2.2 数据格式

每条数据是一个多选题：

```json
{
  "id": "test-0001",
  "question": "下列哪项不是冠心病的危险因素？",
  "A": "高血压",
  "B": "高脂血症",
  "C": "规律运动",
  "D": "吸烟",
  "answer": "C"
}
```

### 2.3 加载方式

使用 HuggingFace `datasets` 库按子集名加载：

```python
from datasets import load_dataset

# 每个子集是 ceval/ceval-exam 下的一个 config
ds = load_dataset("ceval/ceval-exam", "physician", split="test")
# ds[0] → {"id": "...", "question": "...", "A": "...", "B": "...", "C": "...", "D": "...", "answer": "C"}
```

> **注意**: CEval 的 test split 就是用来评测的——因为模型训练时不应该见过这些题。这保证了评测的公平性。

---

## 3. 评测方法

### 3.1 Prompt 构造

将原始多选题格式化为模型能理解的 Prompt。核心设计原则：

1. **角色定位**：明确告诉模型这是一道医学选择题考试
2. **选项清晰**：A/B/C/D 每个选项独占一行
3. **引导输出**：以"答案: "结尾，引导模型直接输出选项字母

```python
prompt = f"""以下是一道医学单项选择题，请选出最正确的答案。

题目: {question}

A. {option_a}
B. {option_b}
C. {option_c}
D. {option_d}

答案: """
```

为什么这样设计？
- **不用 few-shot**：避免示例引导模型偏向特定回答模式
- **"答案: " 引导**：让模型自然输出字母，而不是长篇大论
- **选项分行**：视觉上清晰，tokenizer 也能正确切分每个选项

### 3.2 Chat Template 包装

Qwen2.5 模型使用 HuggingFace 内置的 chat template 进行对话格式化。我们在原始 prompt 外再套一层标准的 user 消息：

```python
messages = [{"role": "user", "content": prompt}]
text = tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=True,  # 自动添加 "<|im_start|>assistant\n"
)
```

Qwen2.5 的 chat template 会将上述 messages 转换为：

```
<|im_start|>system
You are Qwen, a helpful assistant.<|im_end|>
<|im_start|>user
以下是一道医学单项选择题...
答案: <|im_end|>
<|im_start|>assistant

```

### 3.3 模型推理

使用 `model.generate()` 以贪心解码方式生成：

```python
with torch.no_grad():
    outputs = model.generate(
        **inputs,
        max_new_tokens=10,      # 只需要输出字母，给10个token足够
        do_sample=False,        # 关闭随机采样，确保确定性输出
        pad_token_id=tokenizer.eos_token_id,
    )
```

关键参数说明：

| 参数 | 值 | 原因 |
|---|---|---|
| `max_new_tokens` | 10 | 只需要输出一个字母，10个 token 绰绰有余 |
| `do_sample` | False | 贪心解码，保证每次评测结果一致（可复现） |
| `temperature` | 不设（默认1.0但do_sample=False时无效） | 确定性输出 |

### 3.4 答案提取

模型输出的是完整文本（如 "C" 或 "答案是C。" 或 "C. 规律运动"），需要从中提取选项字母：

```python
generated = tokenizer.decode(outputs[0][input_len:], skip_special_tokens=True).strip()
# generated → "C" 或 "答案是 C。" 或 "C. 规律运动"

# 提取第一个出现的 A/B/C/D
pred = None
for char in generated.upper():
    if char in ("A", "B", "C", "D"):
        pred = char
        break
```

为什么不用更复杂的解析（如正则）？

- 大多数情况下模型直接输出单个字母
- 遍历第一个出现的 A-D 字符足够鲁棒
- 如果模型完全不输出 A-D（极端情况），则 `pred = None`，自动判错

### 3.5 准确率计算

```python
is_correct = (pred == ground_truth)  # 模型输出的字母 == 数据集中的 "answer" 字段
accuracy = correct / total
```

每个子集独立计算 accuracy，最后求综合 accuracy = 三个子集的总 correct / 总 total。

---

## 4. 为什么选用多选题评测（而非生成式评测）

### 4.1 多选题评测的优势

| 特性 | 多选题评测 | 自由生成评测 |
|---|---|---|
| **客观性** | 答案唯一，无歧义 | 需要人/LLM 判断对错 |
| **可复现** | 同一模型每次结果一致（贪心解码） | 长文本具有随机性 |
| **成本** | 推理快，无需额外 Judge | 通常需要 GPT-4 等做 Judge |
| **灵敏度** | 能检测细粒度知识变化 | 对风格变化更敏感，知识变化不敏感 |
| **面试认可度** | CEval/CMMLU 是学术界通用基准 | 自建指标需要更多解释成本 |

### 4.2 局限性

- 多选题只能测"知不知道正确答案"，不能测"能不能写出好的回答"
- 不能反映模型的沟通能力、同理心、安全边界意识
- 因此本项目的评测体系中，多选题只是**自动评测层**，还需要配合**人工评测**和**安全评测**组成完整证据链

---

## 5. 消融分析原理

消融实验（Ablation Study）的核心思想：**控制变量，逐个证明每个改动都有正向贡献**。

### 5.1 实验设计

| 对比对 | 变量 | 要证明什么 |
|---|---|---|
| baseline vs sft_selected | 有无 SFT 微调 | SFT 微调是否提升了医学知识 |
| sft_random vs sft_selected | 数据筛选策略 | 向量召回筛选是否优于随机采样 |
| sft_selected vs sft_selected_safety | 有无安全数据 | 安全数据是否损害/提升医学能力 |
| sft_selected vs dpo_selected | 有无偏好优化 | DPO 是否进一步提升 |

### 5.2 对比计算

```python
# 对每个子集分别计算变化量
for subset in ["basic_medicine", "clinical_medicine", "physician"]:
    acc_a = result_a[subset]["accuracy"]   # 如 sft_random 的 physician 准确率
    acc_b = result_b[subset]["accuracy"]   # 如 sft_selected 的 physician 准确率
    delta = acc_b - acc_a                  # 正数 = 改进，负数 = 退化
```

为什么分三个子集而不是只看综合？
- 如果 physician 上升但 basic_medicine 下降，说明模型在"临床应用"上变强了但"基础理论"变弱了
- 分项分析能定位改进/退化的具体来源
- 面试中展示分项分析比只说"整体提升了"更有说服力

---

## 6. 代码调用关系

```
05_run_eval.py                        # 单次评测入口
├── load_ceval_medical_subsets()      #   从 HF 加载 3 个医学子集
├── load_model_and_tokenizer()        #   加载基座模型 + 可选 LoRA
├── evaluate_multichoice()            #   对每个子集跑多选题评测
│   ├── build_prompt()                #     构造多选题 prompt
│   ├── tokenizer.apply_chat_template #     包装为 Qwen chat format
│   ├── model.generate()              #     贪心解码生成
│   └── 字母提取 + 比对               #     判定对错
└── summary.json                      #   输出评测结果

06_collect_results.py                 # 汇总 + 对比 + 消融分析
├── load_summary() × N                #   读取各实验的 summary.json
├── generate_report()                 #   生成 Markdown 对比报告
│   ├── 主结果表 (各子集 accuracy %)
│   ├── 详细数据表 (correct/total)
│   ├── 消融分析 (逐对计算 delta)
│   └── 实验配置表
└── eval_report.md                    #   输出报告
```

---

## 7. 关键设计决策

1. **不用 lm-eval 而直接调用 datasets + transformers**：lm-eval 的任务命名不稳定、版本依赖多，直接加载 HF 数据集更可控、更透明。

2. **贪心解码而非采样**：保证评测结果 100% 可复现。同一模型同一数据每次结果相同，这对科学实验至关重要。

3. **只取第一个字母而非正则解析全部输出**：模型通常直接输出字母，复杂解析反而引入 bug。简单规则反而更鲁棒。

4. **分项 + 综合双报告**：既能快速看整体趋势（综合 accuracy），也能定位具体改进来源（分项 accuracy）。

5. **每次评测独立保存完整 details**：每个子集的每道题结果（期望答案、模型输出、是否正确）都保存为 JSON，支持事后深入分析（如错误集分析、混淆矩阵）。
