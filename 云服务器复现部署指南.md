# 云服务器复现部署指南

## 1. 推荐云服务器配置

| 级别 | GPU | 显存 | 内存 | 适合 |
|---|---|---|---|---|
| 最小闭环 | RTX 4090 / L20 / A10 | 24GB | 32GB | Qwen2.5-0.5B/1.5B LoRA SFT + 评测 |
| 简历展示版 | L40S / A6000 | 48GB | 64GB | Qwen2.5-3B LoRA SFT + DPO + 消融 |

- 系统: Ubuntu 22.04
- CUDA: 12.1+
- 系统盘: 100GB / 数据盘: 200GB+

## 2. 一键初始化脚本

SSH 登录后，复制以下命令逐步执行：

### 2.1 基础环境

```bash
# 创建项目目录 (放在数据盘)
mkdir -p /data/medicalchat && cd /data/medicalchat

# 安装基础工具
apt update && apt install -y git git-lfs tmux htop unzip build-essential
git lfs install

# 安装 Miniconda (如已安装可跳过)
wget -q https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p /opt/conda
/opt/conda/bin/conda init bash
source ~/.bashrc

# 创建 Python 环境
conda create -n medical python=3.10 -y && conda activate medical
```

### 2.2 拉取代码 + 安装依赖

```bash
cd /data/medicalchat
git clone --recursive https://github.com/1angyyshuo/medical.git
cd medical
bash setup.sh          # 安装全部依赖
pip install "lm_eval[hf]"
```

### 2.3 配置 HuggingFace 镜像 (国内服务器)

```bash
echo 'export HF_HOME=/data/medicalchat/cache/huggingface' >> ~/.bashrc
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
```

### 2.4 环境自检

```bash
nvidia-smi
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
python -c "import transformers, peft, trl; print('ok')"
lm_eval --help | head -5
```

## 3. 完整实验流程

全部在 tmux 中运行，防止 SSH 断开：

```bash
tmux new -s medical
cd /data/medicalchat/medical
conda activate medical
```

### Phase 0: Baseline 评测 (30min-1h)

> 先拿到基座模型在医学评测上的原始分数，之后所有改进都跟这个比。

```bash
# 运行 baseline 评测 (自动加载 ceval/ceval-exam 医学子集)
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --name baseline
```

评测数据集: [ceval/ceval-exam](https://huggingface.co/datasets/ceval/ceval-exam)
- basic_medicine (基础医学)
- clinical_medicine (临床医学)
- physician (医师资格考试)

### Phase 1: 数据构造 (2min)

```bash
# Step 1: 下载公开医学数据
bash scripts/download_data.sh

# Step 2: 构造训练集（原始数据直接转格式，不做清洗）
python scripts_project/build_sft_data.py \
    --general MedicalGPT/data/sft/sharegpt_zh_1K_format.jsonl \
    --general_n 1000 \
    --medical_n 2000
```

### Phase 2: SFT 训练

```bash
# 修改 MedicalGPT/scripts/run_sft.sh:
#   --train_file_dir 改为 project_data/sft/
#   --model_name_or_path 改为 Qwen/Qwen2.5-3B-Instruct

bash MedicalGPT/scripts/run_sft.sh
```

**注意**：按 Codex 计划，可以先用小模型 `Qwen/Qwen2.5-0.5B-Instruct` 快速跑通最小闭环验证流程。

### Phase 3: DPO 偏好优化

```bash
# 使用 MedicalGPT 原版 DPO 脚本
bash MedicalGPT/scripts/run_dpo.sh
```

> 修改 `--peft_path` 指向 SFT 输出的 LoRA checkpoint。

### Phase 4: 评测闭环

```bash
# 基线评测（已有结果: overall 27.8%）
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --name baseline

# SFT 模型评测
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --peft <SFT checkpoint 路径> \
    --name sft_raw

# 收集结果
python scripts_project/06_collect_results.py
```

## 4. 训练监控

### SwanLab（云端实验追踪）

注册获取 API Key: https://swanlab.cn

```bash
# 设置 API Key（训练脚本会自动读取）
export SWANLAB_API_KEY="你的API_KEY"
export SWANLAB_PROJECT="medical-chat"

# 训练时 loss/accuracy/lr 等指标自动上传到 SwanLab 云端
# 浏览器打开 https://swanlab.cn 即可实时查看训练曲线
```

### GPU 监控

```bash
# GPU 使用率
watch -n 1 nvidia-smi
```

## 5. 显存不够时

修改 `MedicalGPT/scripts/run_sft.sh` 中的对应参数：
- `--per_device_train_batch_size 1`
- `--gradient_accumulation_steps 16`
- `--model_max_length 512`
- `--lora_rank 4`
- 或使用 QLoRA: `--quantization_bit 4`

或换更小模型：`Qwen/Qwen2.5-0.5B-Instruct`。

## 6. 最终交付物

云服务器上跑完后，`outputs/` 和 `reports/` 目录应包含：

```
outputs/
├── sft/sft_random/          # SFT checkpoint
├── sft/sft_selected/        # SFT checkpoint
├── sft/sft_selected_safety/ # SFT checkpoint
├── dpo/sft_selected/        # DPO checkpoint
└── eval/
    ├── baseline/            # 基线评测结果
    ├── sft_random/          # SFT 评测结果
    ├── sft_selected/        # SFT 评测结果
    └── dpo_selected/        # DPO 评测结果

reports/
├── experiment_log.md        # 实验记录
├── result_table.csv         # 结果表格
├── case_study.md            # 回答对比
└── eval_report.md           # 自动生成的评测报告
```

## 7. 实验记录表 (手动填写)

评测数据: ceval/ceval-exam

| 实验 | basic_medicine | clinical_medicine | physician | 综合 | 备注 |
|---|---|---|---|---|---|
| Baseline | 37.7% | 23.5% | 25.7% | 27.8% | 基座模型原始分数 |
| SFT-random | | | | | 对照组 |
| SFT-selected | | | | | | 向量筛选 |
| SFT-safety | | | | | | +安全数据 |
| DPO | | | | | | 偏好优化 |
