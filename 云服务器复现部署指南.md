# 云服务器复现部署指南

## 1. 推荐云服务器配置

| 级别 | GPU | 显存 | 内存 | 适合 |
|---|---|---|---|---|
| 最小闭环 | RTX 4090 / L20 / A10 | 24GB | 32GB | Qwen2.5-0.5B/1.5B LoRA SFT + 评测 |
| 简历展示版 | L40S / A6000 | 48GB | 64GB | Qwen2.5-3B LoRA SFT + DPO + 消融 |

- 系统: Ubuntu 22.04
- CUDA: 12.1+
- 系统盘: 100GB / 数据盘: 200GB+

## 2. 一键初始化脚本

SSH 登录后，复制以下命令逐步执行：

### 2.1 基础环境

```bash
# 创建项目目录 (放在数据盘)
mkdir -p /data/medicalchat && cd /data/medicalchat

# 安装基础工具
apt update && apt install -y git git-lfs tmux htop unzip build-essential
git lfs install

# 安装 Miniconda (如已安装可跳过)
wget -q https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p /opt/conda
/opt/conda/bin/conda init bash
source ~/.bashrc

# 创建 Python 环境
conda create -n medical python=3.10 -y && conda activate medical
```

### 2.2 拉取代码

```bash
cd /data/medicalchat

# 克隆我们的项目
git clone https://github.com/1angyyshuo/medical.git
cd medical

# 克隆 MedicalGPT 参考代码 (训练依赖)
git clone https://github.com/shibing624/MedicalGPT.git
```

### 2.3 安装依赖

```bash
cd /data/medicalchat/medical

# 基础依赖
pip install -U pip
pip install -e ".[gpu]"          # 含 flash-attn, bitsandbytes
pip install "lm_eval[hf]"
pip install tensorboard sentence-transformers

# MedicalGPT 依赖
pip install -r MedicalGPT/requirements.txt
```

### 2.4 配置 HuggingFace 镜像 (国内服务器)

```bash
echo 'export HF_HOME=/data/medicalchat/cache/huggingface' >> ~/.bashrc
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
```

### 2.5 环境自检

```bash
nvidia-smi
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
python -c "import transformers, peft, trl; print('ok')"
lm_eval --help | head -5
```

## 3. 完整实验流程

全部在 tmux 中运行，防止 SSH 断开：

```bash
tmux new -s medical
cd /data/medicalchat/medical
conda activate medical
```

### Phase 0: Baseline 评测 (30min-1h)

> 先拿到基座模型在医学评测上的原始分数，之后所有改进都跟这个比。

```bash
# 运行 baseline 评测 (自动加载 ceval/ceval-exam 医学子集)
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --name baseline
```

评测数据集: [ceval/ceval-exam](https://huggingface.co/datasets/ceval/ceval-exam)
- basic_medicine (基础医学)
- clinical_medicine (临床医学)
- physician (医师资格考试)

### Phase 1: 数据构造 (10min)

```bash
# Step 1: 构建锚点 (已内置59条医疗锚点)
python scripts_project/01_build_anchor.py

# Step 2: 下载公开医学数据
bash scripts/download_data.sh

# Step 3: 向量召回 + 过滤筛选
# (需要在有 sentence-transformers 的环境运行)
python scripts_project/02_filter_medical_data.py \
    --recall_top_k 10000

# Step 4: 构建4组 SFT 数据集
python scripts_project/03_build_sft.py \
    --general_n 1000 \
    --medical_n 2000 \
    --safety_n 500

# Step 5: 构建偏好数据
python scripts_project/04_build_preference.py
```

### Phase 2: SFT 对照训练

```bash
# 对照组: 随机医疗数据
bash scripts/run_sft.sh sft_random

# 实验组: 向量筛选医疗数据
bash scripts/run_sft.sh sft_selected

# 消融组: +安全数据
bash scripts/run_sft.sh sft_selected_safety
```

**注意**：按 Codex 计划，可以先用小模型 `Qwen/Qwen2.5-0.5B-Instruct` 快速跑通最小闭环验证流程。

### Phase 3: DPO 偏好优化

```bash
bash scripts/run_dpo.sh sft_selected
```

### Phase 4: 评测闭环

```bash
# 基线评测
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --name baseline

# 评测每个 SFT 模型
for exp in sft_random sft_selected sft_selected_safety; do
    python scripts_project/05_run_eval.py \
        --model Qwen/Qwen2.5-3B-Instruct \
        --peft outputs/sft/${exp} \
        --name ${exp}
done

# DPO 评测
python scripts_project/05_run_eval.py \
    --model Qwen/Qwen2.5-3B-Instruct \
    --peft outputs/dpo/sft_selected \
    --name dpo_selected

# 收集所有结果，生成对比报告 (含消融分析)
python scripts_project/06_collect_results.py
```

## 4. 训练监控

```bash
# GPU 监控
watch -n 1 nvidia-smi

# 启动 TensorBoard
tensorboard --logdir outputs --host 0.0.0.0 --port 6006

# 本地通过 SSH 隧道访问:
# ssh -L 6006:127.0.0.1:6006 root@你的服务器IP
# 浏览器打开 http://127.0.0.1:6006
```

## 5. 显存不够时

优先调整 `scripts/run_sft.sh` 中的参数：

```bash
BATCH_SIZE=1 \
GRAD_ACCUM=16 \
MAX_LENGTH=512 \
LORA_RANK=4 \
USE_QLORA=true \
bash scripts/run_sft.sh sft_selected
```

或换更小模型：`Qwen/Qwen2.5-0.5B-Instruct`，用 `--model_max_length 512`。

## 6. 最终交付物

云服务器上跑完后，`outputs/` 和 `reports/` 目录应包含：

```
outputs/
├── sft/sft_random/          # SFT checkpoint
├── sft/sft_selected/        # SFT checkpoint
├── sft/sft_selected_safety/ # SFT checkpoint
├── dpo/sft_selected/        # DPO checkpoint
└── eval/
    ├── baseline/            # 基线评测结果
    ├── sft_random/          # SFT 评测结果
    ├── sft_selected/        # SFT 评测结果
    └── dpo_selected/        # DPO 评测结果

reports/
├── experiment_log.md        # 实验记录
├── result_table.csv         # 结果表格
├── case_study.md            # 回答对比
└── eval_report.md           # 自动生成的评测报告
```

## 7. 实验记录表 (手动填写)

| 实验 | CEval-med | CMMLU-med | 自建QA | 安全得分 | PPL | 备注 |
|---|---|---|---|---|---|---|
| Baseline | | | | | | 基座模型原始分数 |
| SFT-random | | | | | | 对照组 |
| SFT-selected | | | | | | 向量筛选 |
| SFT-safety | | | | | | +安全数据 |
| DPO | | | | | | 偏好优化 |
