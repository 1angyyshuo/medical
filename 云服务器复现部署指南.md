# 云服务器复现部署指南

## 1. 推荐云服务器配置

### 最小闭环

- GPU：1 张 24GB 显存，例如 RTX 4090 / L20 / A10 / A5000
- CPU：8 核以上
- 内存：32GB 以上
- 系统盘：100GB
- 数据盘：200GB-500GB
- 系统：Ubuntu 22.04
- CUDA：12.1 或 12.4

适合跑：

- Qwen2.5-0.5B / Qwen3.5-0.8B
- LoRA SFT
- 小规模 DPO / GRPO
- lm-eval 自动评测

### 简历展示版

- GPU：1-2 张 48GB 显存，例如 L40S / A6000 / A40
- 内存：64GB 以上
- 数据盘：500GB+

适合跑：

- Qwen2.5-1.5B / Qwen2.5-3B
- QLoRA / LoRA SFT
- DPO / GRPO
- 更完整消融实验

## 2. 云服务器目录规划

建议统一放在数据盘，避免系统盘爆掉。

```bash
/data/medicalchat/
  MedicalGPT/
  project_data/
    raw/
    anchors/
    sft_random/
    sft_selected/
    preference/
    grpo/
    eval_private/
  models/
  cache/
    huggingface/
    modelscope/
  runs/
    baseline/
    sft_random/
    sft_selected/
    dpo/
    grpo/
  reports/
```

## 3. 基础环境安装

### 连接服务器

```bash
ssh root@你的服务器IP
```

### 创建项目目录

```bash
mkdir -p /data/medicalchat
cd /data/medicalchat
```

### 安装基础工具

```bash
apt update
apt install -y git git-lfs tmux htop nvtop unzip build-essential
git lfs install
```

### 安装 Conda

如果镜像自带 Conda，可以跳过。

```bash
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
source ~/.bashrc
```

### 创建环境

```bash
conda create -n medicalgpt python=3.10 -y
conda activate medicalgpt
```

## 4. 拉取代码

```bash
cd /data/medicalchat
git clone https://github.com/shibing624/MedicalGPT.git
cd MedicalGPT
```

安装依赖：

```bash
pip install -U pip
pip install -r requirements.txt
pip install "lm_eval[hf]"
pip install tensorboard
```

如果国内服务器访问 Hugging Face 慢，可以额外配置：

```bash
export HF_HOME=/data/medicalchat/cache/huggingface
export TRANSFORMERS_CACHE=/data/medicalchat/cache/huggingface
export HF_ENDPOINT=https://hf-mirror.com
```

建议写进 `~/.bashrc`：

```bash
echo 'export HF_HOME=/data/medicalchat/cache/huggingface' >> ~/.bashrc
echo 'export TRANSFORMERS_CACHE=/data/medicalchat/cache/huggingface' >> ~/.bashrc
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
```

## 5. 先做环境自检

```bash
nvidia-smi
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
python -c "import transformers, peft, trl; print('ok')"
lm_eval --help
```

## 6. 上传本地计划和数据

从本机上传到服务器：

```bash
scp E:/Medicalchat/医疗大模型闭环复现计划.md root@你的服务器IP:/data/medicalchat/
scp E:/Medicalchat/云服务器复现部署指南.md root@你的服务器IP:/data/medicalchat/
```

如果已经有本地构造好的数据：

```bash
scp -r E:/Medicalchat/project_data root@你的服务器IP:/data/medicalchat/
```

如果还没有数据，先用 MedicalGPT 自带样例跑通最小链路。

## 7. 最小 SFT 跑通

进入 tmux，避免 SSH 断开导致训练中断：

```bash
tmux new -s sft
cd /data/medicalchat/MedicalGPT
conda activate medicalgpt
```

先跑小样本 SFT：

```bash
CUDA_VISIBLE_DEVICES=0 torchrun --nproc_per_node 1 training/supervised_finetuning.py \
  --model_name_or_path Qwen/Qwen3.5-0.8B \
  --train_file_dir ./data/sft \
  --validation_file_dir ./data/sft \
  --per_device_train_batch_size 2 \
  --per_device_eval_batch_size 1 \
  --do_train \
  --do_eval \
  --use_peft True \
  --max_train_samples 1000 \
  --max_eval_samples 100 \
  --model_max_length 512 \
  --num_train_epochs 1 \
  --learning_rate 2e-5 \
  --warmup_steps 5 \
  --weight_decay 0.05 \
  --logging_strategy steps \
  --logging_steps 10 \
  --eval_steps 100 \
  --eval_strategy steps \
  --save_steps 500 \
  --save_strategy steps \
  --save_total_limit 3 \
  --gradient_accumulation_steps 8 \
  --preprocessing_num_workers 4 \
  --output_dir /data/medicalchat/runs/sft_smoke \
  --target_modules all \
  --lora_rank 8 \
  --lora_alpha 16 \
  --lora_dropout 0.05 \
  --torch_dtype bfloat16 \
  --bf16 \
  --report_to tensorboard \
  --gradient_checkpointing True \
  --cache_dir /data/medicalchat/cache/huggingface
```

tmux 常用：

```bash
Ctrl+b 然后按 d    # 挂起会话
tmux attach -t sft # 回到训练窗口
```

## 8. 训练监控

查看 GPU：

```bash
watch -n 1 nvidia-smi
```

查看日志：

```bash
tail -f /data/medicalchat/runs/sft_smoke/trainer_state.json
```

启动 TensorBoard：

```bash
tensorboard --logdir /data/medicalchat/runs --host 0.0.0.0 --port 6006
```

本地访问前需要在云厂商安全组开放 6006，或者用 SSH 隧道：

```bash
ssh -L 6006:127.0.0.1:6006 root@你的服务器IP
```

然后浏览器打开：

```text
http://127.0.0.1:6006
```

## 9. lm-eval 基线评测

先列任务，确认当前版本的任务名：

```bash
lm_eval ls tasks | grep -i ceval
lm_eval ls tasks | grep -i cmmlu
```

基线模型：

```bash
lm_eval --model hf \
  --model_args pretrained=Qwen/Qwen3.5-0.8B,dtype=bfloat16 \
  --tasks ceval-valid \
  --device cuda:0 \
  --batch_size auto \
  --output_path /data/medicalchat/runs/baseline_ceval.json
```

SFT 后 LoRA：

```bash
lm_eval --model hf \
  --model_args pretrained=Qwen/Qwen3.5-0.8B,peft=/data/medicalchat/runs/sft_smoke,dtype=bfloat16 \
  --tasks ceval-valid \
  --device cuda:0 \
  --batch_size auto \
  --output_path /data/medicalchat/runs/sft_smoke_ceval.json
```

## 10. 正式实验顺序

建议按这个顺序跑，最稳：

1. `baseline`：原模型评测。
2. `sft_random`：通用 1k + 随机医疗 2k。
3. `sft_selected`：通用 1k + 向量筛选医疗 2k。
4. `sft_safety`：`sft_selected` + 医疗安全拒答 500。
5. `dpo`：基于 `sft_selected` 或 `sft_safety` 做 DPO。
6. `grpo`：基于医学选择题/可验证问答做 GRPO。
7. `ablation`：汇总所有自动评测、人工评测和 case study。

## 11. 常见问题

### 显存不够

优先改这些参数：

```bash
--per_device_train_batch_size 1
--gradient_accumulation_steps 16
--model_max_length 512
--lora_rank 4
--gradient_checkpointing True
```

仍然不够时：

- 换更小模型。
- 开 QLoRA。
- 降低 `max_train_samples`。
- 暂时不跑 DPO/GRPO，先把 SFT + 评测闭环打通。

### 下载慢

配置：

```bash
export HF_ENDPOINT=https://hf-mirror.com
export HF_HOME=/data/medicalchat/cache/huggingface
```

或者提前本地下载模型，再用 `scp` 上传到 `/data/medicalchat/models/`。

### SSH 断了

所有训练都放到 `tmux` 里跑。

### 训练完不知道看什么

至少保存：

- 训练命令
- `trainer_state.json`
- TensorBoard 截图
- lm-eval JSON 结果
- 自建评测 CSV
- 5-10 条 case study

## 12. 云上最终交付物

```text
/data/medicalchat/
  reports/
    experiment_log.md
    result_table.csv
    case_study.md
  runs/
    baseline_ceval.json
    sft_random_eval.json
    sft_selected_eval.json
    dpo_eval.json
    grpo_eval.json
```

最终能讲清楚：

- 我怎么构造数据。
- 我怎么训练模型。
- 我怎么评测模型。
- 哪个组件真的带来了收益。
- 哪些实验失败过，怎么调参解决。

